{
  "name": "xmt-cli-contracts",
  "version": "1.0.0",
  "description": "VectorRegistry smart contract deployment",
  "scripts": {
    "deploy:sepolia": "hardhat run scripts/deploy_vector_registry.js --network sepolia",
    "deploy:base-sepolia": "hardhat run scripts/deploy_vector_registry.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^16.3.1"
  }
}
